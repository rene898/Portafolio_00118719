#include <cstdlib>
#include <iostream>
#include <conio.h>
#include <stdio.h>

using namespace std;
struct theworld{
	char nombre[30];
	char capital[20];
	int habitantes;
	}continentes[5];

int main()
{
    for (int i=0; i<5; i++){
		fflush(stdin);
        cout<<"nombre del pais: "<<endl;
        cin>>continentes[i].nombre;
    }
    
    for (int i=0; i<5; i++){
        cout<<"capital del pais "<<continentes[i].nombre<<endl;
        cin>>continentes[i].capital;
    }
    for (int i=0; i<5; i++){
        cout<<"habitantes del pais: "<<continentes[i].nombre<<endl;
        cin>>continentes[i].habitantes;
}

	    
    for (int i=0; i<5; i++){
        fflush(stdin);
        cout<<"nombre del pais: "<<endl;
        cin>>continentes[i].nombre;
    }

    for (int i=0; i<5; i++){
        cout<<"capital del pais "<<continentes[i].nombre<<endl;
        cin>>continentes[i].capital;
    }
    for (int i=0; i<5; i++){
        cout<<"habitantes del pais: "<<continentes[i].nombre<<endl;
        cin>>continentes[i].habitantes;
}
        
        
    for (int i=0; i<5; i++){
        fflush(stdin);
        cout<<"nombre del pais: "<<endl;
        cin>>continentes[i].nombre;
    }

    for (int i=0; i<5; i++){
        cout<<"capital del pais "<<continentes[i].nombre<<endl;
        cin>>continentes[i].capital;
    }
    for (int i=0; i<5; i++){
        cout<<"habitantes del pais: "<<continentes[i].nombre<<endl;
        cin>>continentes[i].habitantes;
}
	
	
	for (int i=0; i<5; i++){
        fflush(stdin);
        cout<<"nombre del pais: "<<endl;
        cin>>continentes[i].nombre;
    }
    for (int i=0; i<5; i++){
        cout<<"capital del pais "<<continentes[i].nombre<<endl;
        cin>>continentes[i].capital;
    }
    for (int i=0; i<5; i++){
        cout<<"habitantes del pais: "<<continentes[i].nombre<<endl;
        cin>>continentes[i].habitantes;;
	}
        
        
    for (int i=0; i<5; i++){
        fflush(stdin);
        cout<<"nombre del pais: "<<endl;
        cin>>continentes[i].nombre;
    }
    for (int i=0; i<5; i++){
        cout<<"capital del pais "<<continentes[i].nombre<<endl;
        cin>>continentes[i].capital;
    }
    for (int i=0; i<5; i++){
        cout<<"habitantes del pais: "<<continentes[i].nombre<<endl;
        cin>>continentes[i].habitantes;;
}
	    
	system ("pause");
	getch();
	return 0;
}
